# Avatar

Asset packs for the member avatar feature — SVG parts, PNG exports and the
generator script — live in `_source/` (gitignored, on disk beside this file).

No PRD or UX blueprint yet: when the requirement lands, copy the shape from
`features/_template/` and distil the docs here. The assets came first.
