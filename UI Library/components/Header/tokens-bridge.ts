// Re-export so ProfileHeader imports one place and `HeaderAction` stays defined beside the
// component it belongs to.
export { HEADER, PROFILE_HEADER, PROFILE_AVATAR_SIZE, type ProfileHeaderType } from './tokens';
export { HeaderAction } from './Header';
